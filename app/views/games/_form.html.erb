<%= form_for(@game) do |f| %>
    <% if @game.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@game.errors.count, "error") %> prohibited this game from being saved:</h2>

          <ul>
            <% @game.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>


    <div class="field">
      <%= f.label :dayNightFreq %><br />
      <%= f.number_field :dayNightFreq %>
    </div>
    <div class="field">
      <%= f.label :kill_radius %><br />
      <%= f.number_field :kill_radius %>
    </div>
    <div class="field">
      <%= f.label :scent_radius %><br />
      <%= f.number_field :scent_radius %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>